import jsonCONTRACT = "backend/escrow-judge.py"MILESTONES_URL = "https://evidence.example/milestones.json"EVIDENCE_URL = "https://evidence.example/unavailable.json"TOTAL = 1_000BOND = 25def deploy_deal(direct_vm, direct_deploy, buyer, seller, platform):    direct_vm.sender = platform    contract = direct_deploy(CONTRACT)    from genlayer.py.types import Address    direct_vm.mock_web(        r".*evidence\.example/milestones\.json",        {"status": 200, "body": '{"milestones":[{"idx":0}]}'},    )    direct_vm.sender = buyer    direct_vm.value = TOTAL    deal_id = contract.open_deal(Address(seller), MILESTONES_URL, TOTAL, False)    direct_vm.value = 0    direct_vm.sender = seller    contract.accept_deal(deal_id)    contract.declare_milestone(deal_id, 0, "Deliver the audited package", 10_000)    return contract, deal_iddef open_dispute(direct_vm, contract, deal_id, buyer):    direct_vm.mock_llm(        r".*Normalize a free-text dispute statement.*",        json.dumps({"summary": "The submitted package does not satisfy the milestone."}),    )    direct_vm.sender = buyer    direct_vm.value = BOND    dispute_id = contract.open_dispute(        deal_id,        0,        "The submitted package is incomplete.",        EVIDENCE_URL,    )    direct_vm.value = 0    return dispute_iddef test_open_deal_requires_exact_funding(    direct_vm, direct_deploy, direct_alice, direct_bob, direct_owner):    direct_vm.sender = direct_owner    contract = direct_deploy(CONTRACT)    direct_vm.mock_web(        r".*evidence\.example/milestones\.json",        {"status": 200, "body": "{}"},    )    direct_vm.sender = direct_alice    direct_vm.value = TOTAL + 1    with direct_vm.expect_revert("must equal total"):        from genlayer.py.types import Address        contract.open_deal(Address(direct_bob), MILESTONES_URL, TOTAL, False)def test_only_seller_can_claim_released_tranche_and_accounting_updates(    direct_vm, direct_deploy, direct_alice, direct_bob, direct_owner):    contract, deal_id = deploy_deal(        direct_vm, direct_deploy, direct_alice, direct_bob, direct_owner    )    direct_vm.sender = direct_bob    contract.request_release(deal_id, 0)    direct_vm.sender = direct_alice    assert contract.release_milestone(deal_id, 0) == TOTAL    before = contract.deal(deal_id)    assert before["allocated"] == TOTAL    assert before["released"] == 0    assert before["seller_claimable"] == TOTAL    with direct_vm.expect_revert("permitted ['SELLER']"):        contract.claim_seller_payout(deal_id)    direct_vm.sender = direct_bob    assert contract.claim_seller_payout(deal_id) == TOTAL    after = contract.deal(deal_id)    assert after["seller_claimable"] == 0    assert after["released"] == TOTAL    assert after["refunded"] == 0    assert after["escrow_balance"] == 0    with direct_vm.expect_revert("no seller payout claimable"):        contract.claim_seller_payout(deal_id)def test_timeout_recovery_is_blocked_before_deadline(    direct_vm, direct_deploy, direct_alice, direct_bob, direct_owner):    direct_vm.warp("2026-07-01T00:00:00Z")    contract, deal_id = deploy_deal(        direct_vm, direct_deploy, direct_alice, direct_bob, direct_owner    )    dispute_id = open_dispute(direct_vm, contract, deal_id, direct_alice)    with direct_vm.expect_revert("recovery not available before"):        contract.recover_timed_out_dispute(deal_id, dispute_id)def test_timeout_recovery_refunds_buyer_and_returns_bond(    direct_vm, direct_deploy, direct_alice, direct_bob, direct_owner):    direct_vm.warp("2026-07-01T00:00:00Z")    contract, deal_id = deploy_deal(        direct_vm, direct_deploy, direct_alice, direct_bob, direct_owner    )    dispute_id = open_dispute(direct_vm, contract, deal_id, direct_alice)    direct_vm.warp("2026-07-08T00:00:01Z")    direct_vm.sender = direct_bob    assert contract.recover_timed_out_dispute(deal_id, dispute_id) == TOTAL    state = contract.deal(deal_id)    dispute = contract.dispute(dispute_id)    assert state["buyer_claimable"] == TOTAL    assert dispute["final_verdict"] == "RECOVERY_REFUND"    assert dispute["recovered"] is True    with direct_vm.expect_revert("permitted ['BUYER']"):        contract.claim_buyer_refund(deal_id)    direct_vm.sender = direct_alice    assert contract.claim_buyer_refund(deal_id) == TOTAL    assert contract.claim_dispute_bond(dispute_id) == BOND    final_state = contract.deal(deal_id)    assert final_state["refunded"] == TOTAL    assert final_state["escrow_balance"] == 0def test_unavailable_evidence_does_not_block_buyer_wins_settlement(    direct_vm,    direct_deploy,    direct_alice,    direct_bob,    direct_charlie,    direct_owner,):    contract, deal_id = deploy_deal(        direct_vm, direct_deploy, direct_alice, direct_bob, direct_owner    )    dispute_id = open_dispute(direct_vm, contract, deal_id, direct_alice)    direct_vm.sender = direct_owner    from genlayer.py.types import Address    contract.assign_arbiter(deal_id, Address(direct_charlie))    direct_vm.sender = direct_charlie    contract.file_arbiter_finding(        deal_id,        dispute_id,        "BUYER_WINS",        "The package is incomplete and should be refunded.",    )    direct_vm.mock_web(        r".*evidence\.example/unavailable\.json",        {"status": 503, "body": "temporarily unavailable"},    )    direct_vm.mock_llm(        r".*synthesize a final verdict for an escrow dispute.*",        json.dumps(            {                "verdict": "BUYER_WINS",                # A contradictory model field must not override the categorical                # winner; the contract canonicalizes BUYER_WINS to zero.                "split_bps": 5_000,                "reasoning": "The arbiter finding supports a full refund.",            }        ),    )    assert contract.finalize_dispute(deal_id, dispute_id) == "BUYER_WINS"    dispute = contract.dispute(dispute_id)    state = contract.deal(deal_id)    assert dispute["evidence_failed"] is True    assert state["buyer_claimable"] == TOTAL    assert state["seller_claimable"] == 0    direct_vm.sender = direct_bob    with direct_vm.expect_revert("only bond recipient may claim"):        contract.claim_dispute_bond(dispute_id)    direct_vm.sender = direct_alice    assert contract.claim_buyer_refund(deal_id) == TOTAL    assert contract.claim_dispute_bond(dispute_id) == BOND